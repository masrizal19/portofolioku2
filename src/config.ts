// ====================================================================
// CENTRALIZED FRONTEND API CONFIGURATION
// For shared hosting, set this to the location where your PHP files reside.
// e.g., 'https://yourdomain.com/api' or keep '/' for relative paths on the same domain.
// ====================================================================

export const API_BASE_URL = ''; // Relative path by default to match hosting directory structures
